#!/usr/bin/env ruby

$0 = "cozy_console #{ARGV[0] || "Interface"}"
DASH_ARGS = ARGV.select {|a| /^--/.match(a) }
ARGV.delete_if {|a| /^--/.match(a) }

require 'irb'
require './hansel'

ARGV.clear
IRB.start
